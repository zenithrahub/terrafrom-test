metadata:
  id: CUSTOM_AWS_405
  name: "Ensure Environment tag exists"
  category: "GENERAL_SECURITY"

definition:
  cond_type: attribute

  resource_types:
    - aws_instance

  attribute: tags.Environment

  operator: exists
