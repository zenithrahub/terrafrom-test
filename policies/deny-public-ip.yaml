metadata:
  id: CUSTOM_AWS_601
  name: "Ensure EC2 instances are not public"
  category: "NETWORK_SECURITY"

definition:
  cond_type: attribute

  resource_types:
    - aws_instance

  attribute: associate_public_ip_address

  operator: not_equals

  value: true
