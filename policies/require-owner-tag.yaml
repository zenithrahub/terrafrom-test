metadata:
  id: CUSTOM_AWS_501
  name: "Ensure Owner tag exists"
  category: "GENERAL_SECURITY"

definition:
  cond_type: attribute

  resource_types:
    - aws_s3_bucket

  attribute: tags.Owner

  operator: exists
