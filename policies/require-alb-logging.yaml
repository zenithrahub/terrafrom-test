metadata:
  id: CUSTOM_AWS_404
  name: "Ensure ALB logging enabled"
  category: "LOGGING"

definition:
  cond_type: attribute

  resource_types:
    - aws_lb

  attribute: access_logs.enabled

  operator: equals

  value: true
