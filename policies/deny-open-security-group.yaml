metadata:
  id: CUSTOM_AWS_402
  name: "Ensure no public SSH access"
  category: "NETWORK_SECURITY"

definition:
  cond_type: attribute

  resource_types:
    - aws_security_group

  attribute: ingress.cidr_blocks

  operator: not_contains

  value: "0.0.0.0/0"
